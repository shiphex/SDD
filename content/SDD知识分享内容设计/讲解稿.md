(开场白)

大家好，这个视频和大家分享一下
我在学习使用 SDD 规格驱动开发时的
一些经验与感悟。

---
(背景介绍)

首先，第一个问题，什么是“规格驱动开发”呢？

规格驱动开发， SDD ，即 Spec-Driven Development。
就是在实际编写代码前，先构建一个足够明确、足够可验证的规格文档，
但这个文档不是越详细越好，否则给人制造大量文档维护成本。
然后通过该文档驱动后续代码编写、测试、迭代的软件开发方法论。

第二个问题，在 AI 时代，为什么需要规格驱动开发？

AI 时代下，代码的产生速度已经成百上千倍的提升了，
但在使用 AI Coding 编写代码时，只要系统变得复杂，
代码量变得庞大，AI Coding 生成的代码就会变得越来越发散，
可读性越来越差，比古法编程时代更快的变成一座屎山。
当然，Spec 自己也可能错，
所以需要把 Spec 当作可验证的假设模型，用现实和实现证据来检验它。

第三个问题，在 AI 时代，初级岗位工作的相对价值越来越低了。

初级岗位的工作内容，
往往是听从领导，根据规格文档进行开发，比如，
“这个功能怎么实现？”，“接口怎么设计？”。
从更高纬度的视角来看，能清楚的看到问题、
能设计恰当的解决方案、能合理评估解决实际问题的效果，
这样的能力在 AI 时代，相对价值更高了。

为了能够更好的与 AI 合作进行设计开发，
并同时拥有更高维度的分析、设计、评估能力，
所以我决定学习并实践 SDD 规格驱动开发。

---
(冲突)

该如何与 AI 协作进行规格驱动开发？

---
(回答)

依据 “Intent → Specification → Design → tasks → Implementation → Verification
” 的思想方法来设计流程

为了实践这一套工作流，
我设计了一套相应的文件体系，
通过这套文件体系，可以实现：
纯古法编程开发、不同程度的 AI 协作开发、纯 AI 编程开发。

当然，也可以不必完全按照这套文件体系来设计。
根据实际情况，可以按需要进行调整。

---
(问题)

已经有 github spec kit、openspec、mattPocock/skills 等相关开源仓库了，
我为什么还自己设计了一套文件体系？

---
(回答)

确实得承认这些有几万十几万标星的仓库更加优秀，
但 SDD 规格驱动开发的方法在 AI 时代十分重要，
所以我需要通过自己至少实操一遍，
来熟悉 SDD 规格驱动开发的方法。
就像是现代已经有了计算器、excel 等工具用于计算，
但我们依然要学习加减乘除、乘法口诀表等基本的数学知识。

而我把我自己的文件体系以及经验感悟分享出来，
是希望够帮助到其他学习者，
与此同时，我也能够从其他学习者那里获得一些反馈，
并根据反馈进行调整。

另外，也是想让我自己的仓库、视频、文章能得到更多的收藏与使用。

---
(问题)

那么，应该怎么做？这套文件体系又是怎么样的？

---
(回答)

践行 SDD 规格驱动开发的核心其实是在人的活动，
人要做的是：
1. 明确目标、非目标和成功标准。
2. 核实当前实现与已有约束。
3. 设计当前增量需要的职责、边界、契约和失败语义。
4. 裁决会影响当前增量的关键问题。
5. 从现状与目标的差距推导最小可验证任务。
6. 在授权范围内实施，并处理新证据带来的变化。
7. 用实际证据验收，同步受影响的规格并刷新当前事实。

而文件系统只是信息地图。

我设计的这套文件体系由两个模块 "Spec 编写"和 "Spec 执行"，及一条反馈回路构成。
"Spec 编写"模块负责保存当前有效规格。
"Spec 执行"模块负责保存当前事实、Gap 与 Tasks。
反馈回路，负责 Feedback / Review / 变化路由。

第一个模块 "Spec 编写"，包含文件：
1 problem.md，用于描述问题，设计目标及成功标准。
2 architecture.md，用于描述系统、模块的架构设计，并规范模块命名。
3 runtime.md，用于描述系统、模块的运行时行为。
4 contracts.md，用于描述模块的接口及相互之间的交互设计。
5 failures.md，用于描述系统、模块的失败策略。
6 decisions.md，用于记录设计决策。
7 test-plan.md，用于描述测试计划。

第二个模块 "Spec 执行"，包含文件：
8 tasks.md，用于描述需要执行的任务，包括总体任务列表、子任务列表、阶段任务、任务详细描述等。
执行结果为 Agent 给出的带上提交哈希、测试日志、代码变动等原始信息的报告。

反馈回路：
9 review.md，用于记录出现问题或需要调整时，通过反馈回路对规格文档进行调整的内容。

这些文件并非按排序进行书写，比如，
你可能在 problem 阶段就做决策，
在 architecture 阶段做决策，
在实现过程中也会产生决策，
然后在产生决策时就应该编写到 06 decisions.md 中。

---
(转场到下一级)

接下来将详细描述每个文件的内容，并给出对应模板。
先看第一个模块 "Spec 编写" 的文件。

---
(描述)

首先，第一个文件是 problem.md，
这个文件首先需要描述背景，也就是需求是什么。
然后要明确当前状态，即系统现有代码的情况，
如果还没开始编码，可以分析一下当前要解决真正的问题是什么。
即“为什么这是问题、问题基线、必要事实、Assumption / Unknown”。

然后是制定设计目标，描述期望得到的可观察的产品结果。
如果希望通过开发学习经验，也可把学习目标等列举出来。
如果想先将最小系统实现，可添加描述阶段开发目标。

在制定完目标后，一定要编写 Non-goal，
填充最容易被 Agent 顺手扩张、但本次不做的内容，
避免 Agent 在编程时顺手实现非目标功能。

然后是编写成功条件，即描述做到什么程度，
我可以宣布这次开发成功，而不是继续“优化”。
相关描述可使用编号，SC-XX，
以便在后续的测试、验收、迭代中引用。
通过自然语言描述成功条件，若不知如何描述，
相关描述可选择使用 Given、When、Then 模板，
即：在什么初始条件下，触发什么操作后，
期望得到什么结果，从而清晰的表达。

最后是对质量属性的描述。
质量属性是指在什么刺激下，系统在什么环境里，以什么响应达到什么度量。
包括可修改性、可测试性、可靠性、延迟等等。

另外，若有暂时无法明确的项目，
或者有会影响设计的情况，
可以在本文档的最后记录。

---
(拓展)

如何从背景或需求中分析出真正的设计目标，
而不是很肤浅的描述。

可以试试 mattpocock 的 grilling skill。
如果希望提高自己分析问题，提出方案的能力，
我推荐学习或让 AI 解读一下《金字塔原理》这本书。

---
(描述)

第二、三、四、五个文件是对代码实际设计的详细描述。

architecture.md 用于制定系统架构及组成，
就像是描述人体有哪些系统、器官、组织，
以及不同层级之间的组成关系。

runtime.md 用于描述系统、模块的运行时行为，
类似于描述人体的新陈代谢、状态变化、信息能量传递等。

contracts.md 用于描述模块之间的交互设计，
如果用人体举例的话，
就像是描述神经和器官生成神经递质、激素或对其的作用，
营养物质、代谢废物的运输和处理等。

failures.md 用于描述系统、模块的失败策略，
类似于描述人体的疾病、伤害以及如何自我调节。

---
(转场到下一级)

首先来看 architecture.md 文件。

---
(描述)

最先出现的是对术语的规范。
该项的目的是为了统一命名，避免命名冲突，
同时避免同一个东西被命名成了多个不同的名称，
导致 Agent 重复设计。
当然，不需要一开始就定义所有的术语，
可随着设计的不断进行不断填充。

然后是系统和模块的架构设计描述。
需要描述其负责的内容，
对外联系哪些模块、接口等。
另外，不负责的内容，也需要说明。
可以使用一些工具绘制出系统架构图，
帮助理解系统架构。

另外，可以使用一些模板，辅助描述系统架构。
如示例中的两个表格，
描述组件的职责、非职责项、所有者、责任方等。
当然，这些模板只是参考，
可根据实际情况进行调整。

最后，这里可以借鉴 ATAM 的思想，做一个轻量架构权衡检查。
可以对某个模块的架构进行评估，
或者当某个模块的架构有多种不同的选择时，
通过评估确定哪种架构更适合当前的需求。
当有复杂的架构选择时，
可转移到 decisions.md 中进行记录与抉择。

无论是系统还是模块都可以按上述方式进行讨论。
详细可以参考或让 AI 参考 C4 的层次化建模思想。

---
(描述)

紧随其后是 runtime.md ，其中记录的主要是，
某某模块或系统的生命周期、创建路径、
销毁链路、数据链路、状态转移流程等。

在描述某某模块运行时行为时，
可使用一些工具绘制出模块运行时的流程图、时序图、转移图等。
需要将重要运行时行为的时序图都绘制出来，
帮助理解模块运行时的行为。
或者说，核心逻辑只要带状态、并发、跨系统调用，或是失败场景复杂，
就绘制出其流程图、时序图、转移图等。
我也给出了一些可以参考的运行时设计表格，当然也可以是流程图：
例如，替代流程与失败流程，
状态转移流程、事件驱动流程等。

模块和系统的运行时流程图都可以按照上述方式进行绘制。

---
(描述)

然后是 contracts.md ，
用于描述模块之间的交互设计。
包括该模块必须设计的跨边界的可依赖语义。
可以是函数、API、Command、Event、Message 等，
以及容易被 Agent 顺手扩张、但本次不做的内容。

还是一样，需要将主要或者全部的模块及系统的交互设计都描述出来，
帮助理解模块之间的交互。

---
(描述)

最后是 failures.md ，
用于描述系统、模块的失败策略。

包括全局失败策略、具体失败策略等。

全局失败策略，具体描述：
定义异常的“语言”，AI 必须知道什么时候该用强类型的领域异常，什么时候向上抛。
指定“谁”才是唯一能发起重试的负责人。
防止“部分执行”污染，如果复合对象只构造了一半就报错，必须要有相应的处置措施。
确保系统退出、或者资源释放操作是安全的。
不能因为清理 A 失败了，就不去清理 B。
防止在非法状态下运行。
未知外部结果处理。

具体失败策略，用于记录详细的失败场景：
包括：编号、失败描述、描述何处失败的检测点、问题的接收方和责任方，
以及相应的处理规则和动作，失败处理的最终状态。
没有的条目可以取消该列。

最后，如果有必须处理的失败场景，
可在最后记录。

---
(拓展)

关于程序模块的设计，
我推荐使用《软件设计哲学》这本书中推荐的深模块设计。

这样不用为了众多小模块，单独设计架构、运行时、交互等，
在 AI coding 时，这样做就可以避免模块调用混乱的问题。

同时避免我们在做架构设计时出现“察察之明”的情况，
就比如《资治通鉴》中，魏文侯明乐音而不明乐官的故事，
但贤明的国君应该去考核、了解‘乐官’（主管音乐的官员）是否称职，
而不是去亲自钻研‘乐音’（具体的音准、乐理）。

同理，在设计架构时，如果使用深模块的设计模式，
用简单接口隐藏大量内部复杂性，
让通过更稳定、更小的跨模块知识面，
让 Contract 更简单，架构认知负担降低
而能更好的把精力放到对架构的设计上。

除非某个深模块效果不行，再深入到该模块中进行细化设计。

---
(返回上一级)

在 2、3、4、5 五个文件对代码实际设计的详细描述之后，
需有文件对架构设计时的一些关键决策进行记录，
以及有文件编写对最终的项目代码的测试评估。
这就需要 decisions.md 和 test-plan.md 文件。

---
(描述)

我们先来看 decisions.md 文件。

该文件记录对架构、质量属性或长期演进产生明显影响的设计决策。

这些决策使用 ADR-xx 编号 + 描述的方式作为标题。
然后填写该决策的状态、提交 ID、决策能影响到什么阶段或者长期有效，
对决策的人工 review。

然后是描述冲突的具体内容，包括：
对需要决策的冲突进行描述，
对当前事实的描述，
不能改变的性质，
驱使该决策的驱动因素，
仍不确定的事实。

然后可以列表给出不同的决策选项，
可根据不可变的性质，核心诉求取舍，
复杂度、可逆性、影响范围进行评估，
得到是否接受该决策。

并得出结论，这项选择获得什么，失去什么，
以及因为这项决策需要新增的内容。

如果决策依然无法得到定论，
则需要说明延迟的原因。

---
(描述)

第一个模块的最后，是 test-plan.md 文件。
首先是整体的测试框架及策略：
首先是单体测试，包括单元测试、状态测试、contract 测试等。
然后是多模块的链路测试。
EtoE 测试，别让别人的错误，耽误了我们自己系统上线。
以及冒烟测试。

然后是明确测试项的状态语义：
包括：通过、失败、环境问题、跳过、未验证。

之后是详细的测试项描述：
每个测试项都要有唯一编号，
然后可以参考模板中的不同类别的测试进行描述。

然后，可以将成功标准链接到 01 problem.md 文件中的对应描述。

最后通过“可追溯性矩阵”
来确保每个测试项都有对应的 Spec、任务、证据等。

---
(返回上一级)

看完上述第一个模块 "Spec 编写" 的文件，
我们可以开始来看第二个模块 "Spec 执行" 的文件。

有 Spec 不等于 SDD。
只有当 Task 从 Spec 推导、实现受 Spec 约束、验收重新回到 Spec，
Spec 才真正“驱动”开发。

---
(描述)

"Spec 执行" 的核心文件是 tasks.md，
在这个文件中，
首先编写总体任务目标，
包括：MVP 最小可交付产品的描述，
以及一些可选目标描述，
另外，需要列举出任务的阶段计划。

然后，要列表说明当前事实以及已接受的设计约束，
当前事实是当前 commit 下实现究竟是什么样、随开发不断变化的事实，
而非 01 problem.md 文件中的“为什么这是问题、问题基线、必要事实、Assumption / Unknown”，
故内容需随着任务的推进而更新；
已接受的设计约束不随任务的推进而更新，
只在有不同设计约束时更新。

之后就是正式的任务描述了，
每个任务都有一个唯一的编号，
以及一个结果式名称。

任务描述的第一项标明任务的类型，
包括：调查、执行、验证等。
然后是任务的状态，
包括：计划中、待开始、进行中、正审阅、已完成、已阻塞等。

之后是任务的目标，
即完成后可以观察到什么结果。

以及任务所需的参考，
即填写所需参考的01-07文件中的具体描述。

而后，是任务的前置条件，
即为什么现在可以开始；未满足项必须阻塞或转调查。

以及任务的范围，
包括：允许改变的职责区域、必要接缝及理由。

填写完以上这些，
就开始描述任务的具体实现，
包括 Must 和 Must not。
这和 01 中的 goal 、 non-goal 类似，
Must 是必须完成的最小事实，
Must not 是最可能发生的范围扩张或边界破坏。

后续，填写任务的验证、转交、设计变更等。

验证中的内容可直接引用 test-plan.md 文件中的测试项。

Handoff 中填写当前执行任务中发现的需要转交的事项。
Design Delta 中填写设计变更的描述。
这两项若没有可以不填写。

该文档最后填写已完成的任务列表。
包括编号、简要描述、对应版本、必要入口，
其中必要入口是指任务完成时，需要记录的必要信息。
这些信息可以帮助后续学习相关知识，
而 Agent 可直接通过 baseline 中的 commit id 来获取相关信息。

这个文件也体现了“过去压缩、现在详细、未来粗略”的原则。

---
(返回上一级)

最后是反馈回路。

---
(描述)

反馈回路的核心是 review.md 文件。
可以是人工 review，也可以是 AI review。
Review 只负责裁决，不负责永久保存最终规则；
最终规则必须回写其对应的文件中。

review 文件通过 DD-xx 编号 + 描述的方式作为标题，
描述需要反馈的内容。

每条反馈内容都需要填写：
Facts 事实是什么？
Decision 决策是什么？
决策包括：Invariant 哪些东西不能牺牲?
Mechanism 用什么机制落实?
Why 为什么这样选？
Deferred 明确延后什么？
Verification 怎么知道这个决定有效？

---
(返回上一级)

至此，我们已经完成了两个模块及反馈回路的讲解。
在此之外，还有些项目长期成立的东西：
依赖方向、测试原则、日志规范、安全规则、核心架构原则等。
这些项目约束指导了 SDD 的整个过程。
而它们一般来自于 AGENTS.md / CONTRIBUTING / constitution 这样的文件中。

---
(拓展)

上述 SDD 规格驱动开发的相关文档中，
我个人认为最重要的是 01 problem.md 和 09 review.md 文件。
因为这两个文件是最能体现人的意志的文件。
发现现实问题，明确问题是什么，
真实情况是这样的吗，思考问题的原因，
想象出问题被解决时的效果，再以此思考如何解决问题，
以及当现实与期望有差距时如何处理，如何反馈调节。
这些问题在 AI 时代变得更重要了，
这个时代做一个“大头兵”的价值已经越来越小了，
甚至等大模型或者其他的 AI 工具越来越强，
很多工具性的工作被逐步替代，
作为人该何去何从？
我个人认为，
相比于做个执行层面的工具人，
应该把自己定位、培养成一个“军师”甚至“君主”，
这种更需要人来思考、判断、决策，
更具主体性的角色。

最后，再分享一个《资治通鉴》中的小故事，
汉昭帝十几岁登上王位，国家大权由霍光掌管，
燕王刘旦、大臣桑弘羊、上官桀嫉妒不满，
乘着霍光离开长安去检阅部队，
便以燕王的口吻写信给汉昭帝，
举报霍光擅自调兵、滥用职权、蓄谋篡位。
但汉昭帝看完举报信后，却发现了破绽，
燕王远在北京，而他和霍光在长安，远隔千里，
就算他有顺风耳，也不可能在短短几天内得知消息，
并把举报信送到长安，所以这封举报信必然是假的。

同理， AI 虽然不会骗我们，
但也经常给我们很多看似流畅的信息，
实际时因为在强化学习时，整个赋分过程类似于应试，
若你不知答案却随意猜测，或许能侥幸答对。而空着不答则必然得零分。
所以面对一个不清楚的问题时，
大模型倾向于给出一个似是而非的答案，而不是说不知道。
再加上现在新起的 GEO (生成式引擎优化)手段，
不排除 AI 检索到的信息质量不高甚至是错误的。
因此，在 AI 时代，人的思考、判断、决策能力更加重要。



``` text
                Project Principles
                        ↓
Problem / Intent / Success Criteria
                        ↓
           Specification & Design
          ↙      ↓       ↓       ↘
   Architecture Runtime Contract Failure
          ↘      ↓       ↓       ↙
              Decisions
                  ↓
            Verification
                  ↓
Current Code → Gap → Task
                  ↓
            Implementation
                  ↓
               Evidence
                  ↓
         Review / Feedback
                  ↓
       修改真正的 Spec Owner
                  ↺
```


































